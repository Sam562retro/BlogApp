
    <div class="form-group mb-4">
        <label for="title">Title: </label>
        <input type="text" name="title" autocomplete="off" required value="{{articleData.title}}" class="form-control" id="title">
    </div>
    <div class="form-group mb-4">
        <label for="description">Description: </label>
        <textarea name="description" class="form-control" required id="description">{{articleData.description}}</textarea>
    </div>
    <div class="form-group mb-4">
        <label for="markdown">Markdown: </label>
        <textarea name="markdown" class="form-control" required id="markdown">{{articleData.markdown}}</textarea>
    </div>
    <a href="/" class="btn btn-warning">Cancel</a>
    <button type="submit" class="btn btn-primary">Save</button>

